// Escribe funciones flecha con parámetros

const myConcat = (arr1, arr2) => {
    return arr1.concat(arr2); // Sirve para unir arrays a una sola array
};
  
console.log(myConcat([1, 2], [3, 4, 5]));