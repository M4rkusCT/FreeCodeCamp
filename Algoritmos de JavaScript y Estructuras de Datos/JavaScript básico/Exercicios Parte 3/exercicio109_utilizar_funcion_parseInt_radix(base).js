// Utiliza la función "parseInt" con Radix (Base)
// La base radix el numero entero tiene que ser entre 2 y 36

function convertToInteger(str) {
    return parseInt(str,2);
}

convertToInteger("10011");