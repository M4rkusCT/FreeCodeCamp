// Captura los paréntesis de apertura y cierre que faltan después de una llamada a una función

function getNine() {
    let x = 6;
    let y = 3;
    return x + y;
}
  
let result = getNine();
console.log(result);